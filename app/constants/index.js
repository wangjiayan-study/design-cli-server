const NAME_SPACE = '.design-cli';
const BUILD_DIR = 'cloud-build';

const SUCCESS = 0;
const FAILED = -1;

module.exports = {
  SUCCESS,
  FAILED,
  STATUS: {
    ON: 1,
    OFF: 0,
  },
};

module.exports = {
  NAME_SPACE,
  BUILD_DIR,
  SUCCESS,
  FAILED,
  STATUS: {
    ON: 1,
    OFF: 0,
  },
};
